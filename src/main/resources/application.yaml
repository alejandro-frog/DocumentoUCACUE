spring:
  application:
    name: registro
  datasource:
    url: jdbc:mysql://localhost:3308/Registro?createDatabaseIfNotExist=true&useSSL=false&allowPublicKeyRetrieval=true
    username: app_user
    password: "123456"
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQLDialect
  thymeleaf:
    cache: false
server:
  port: 8080